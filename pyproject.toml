[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "survive-bros"
version = "1.0.0"
description = "A Pygame survival game"
requires-python = ">=3.9"
dependencies = [
    "pygame>=2.2.0",
]

[tool.pygbag]
build-dir = "build"
